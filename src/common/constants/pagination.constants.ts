export const DEFAULT_PAGE = 1;
export const DEFAULT_LIMIT = 10;
export const MAX_LIMIT = 100; // Set a maximum limit to prevent performance issues - this may be adjusted based on the application's requirements and tradeoffs between performance and memory usage
